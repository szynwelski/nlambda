+ dowiedzieć się co to jest D:... -- DataCon dla klasy
+ dowiedzieć się do czego służą funkcje: $c..., $f..., 
+ zaimplementować najpierw mapy wszystkich nazw (TyVar, DictId, DataCon, Var,) a później klasy, zmienne i in. przekształcić wzajemnie rekurencyjnie
+ zaimplementować plugin bez wprowadzania nowych klas (NLambda...), -- chyba jednak są potrzebne (np. jak klasa ma metodę a -> Atom)
+ obsłużyć deriving, Show
+ obsłużyć import innych modułów,
+ zmienić unionMeta na :: WithMeta a -> WithMeta b -> (a,b,Meta)
- dodać obowiązkową klasę (dla replace vars) dla metod WithMeta
- zaimplementować NLambdaPrelude,
- zaimplementować NLambdaAtom (dla testów)
